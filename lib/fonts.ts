/**
 * Font configuration for LegisClara.
 *
 * Uses system font stack for optimal performance.
 * To use Inter from Google Fonts, replace with:
 *   import { Inter } from "next/font/google";
 *   export const sans = Inter({ subsets: ["latin"], variable: "--font-sans" });
 */
export const fontSansClass = "";
export const fontSansVariable = "--font-sans";
